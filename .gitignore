node_modules/
dist/
build/

# Local database (SQLite) — see ADR-001
server/data/

# Logs
*.log
npm-debug.log*

# Env
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
.idea/
.vscode/
*.tsbuildinfo
.vercel

# Added by the Vercel CLI. `.env*` is deliberately broad — it catches
# `.env.production`, `.env.local` and anything else pulled from Vercel — so the
# example files, which are documentation and *must* stay committable, are
# re-included below.
.env*
!.env.example
!*.env.example
